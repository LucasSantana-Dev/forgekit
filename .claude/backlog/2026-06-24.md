# Backlog — forgekit — 2026-06-24

Generated by `/backlog` composite. 25 items, ROI-ranked, critic-reviewed.

## Phase 1 — High Severity

- [ ] Add Playwright E2E to CI validate workflow
  - category: test-gap | severity: high | effort: s | ROI: 1.5
  - evidence: .github/workflows/validate.yml (no e2e job)
  - approach: Add a new `e2e` job to validate.yml that installs Playwright + Chromium, runs `pnpm --filter @forge-kit/web run test:e2e`, caches browser binaries.

- [ ] Add unit tests for CLI install command
  - category: test-gap | severity: high | effort: m | ROI: 1.0
  - evidence: packages/cli/src/commands/install.ts (no tests)
  - approach: Jest/tsx tests for happy path (skill/hook/agent/tool install), missing entry error, --force flag, already-installed guard.

- [ ] Add unit tests for CLI uninstall command
  - category: test-gap | severity: high | effort: s | ROI: 1.5
  - evidence: packages/cli/src/commands/uninstall.ts (no tests)
  - approach: Test each kind's uninstall path, missing entry behavior, and idempotent re-uninstall.

- [ ] Add unit tests for CLI setup/list commands
  - category: test-gap | severity: high | effort: s | ROI: 1.5
  - evidence: packages/cli/src/commands/setup.ts, list.ts (no tests)
  - approach: Test list filtering (--kind, --tag, --query), setup adapter selection, and error paths.

## Phase 2 — Medium Severity

- [ ] Enforce locale parity in CI (remove FORGE_KIT_SKIP_I18N_CHECK bypass)
  - category: feature | severity: medium | effort: xs | ROI: 2.0
  - evidence: packages/catalog/scripts/validate-catalog.ts:173
  - approach: Remove or gate the FORGE_KIT_SKIP_I18N_CHECK env var; enforce i18n validation unconditionally in CI.

- [ ] Add tutorial kind to CLI IndexEntry + generate-index
  - category: tech-debt | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/cli/src/lib/catalog.ts (IndexEntry.kind missing "tutorial"), packages/catalog/scripts/generate-index.ts
  - approach: Add "tutorial" to IndexEntry kind union; update generate-index.ts to include tutorials; add "tutorial" to countByKind in list.ts.

- [ ] Validate gateway URL format (http/https) in add-server command
  - category: security | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/cli/src/commands/add-server.ts:52
  - approach: Parse URL with `new URL()` and reject non-http(s) schemes before sending to gateway.

- [ ] Investigate and fix 2 failing Dependabot CI runs on main
  - category: infra | severity: medium | effort: s | ROI: 1.0
  - evidence: .github/ (npm_and_yarn Update, github_actions Update — both "failure" on main)
  - approach: Review Dependabot update PRs, check if they're blocked by config or conflicts; resolve or close stale runs.

- [ ] Demote uninstall "not installed" logs (false success signal)
  - category: dx | severity: medium | effort: xs | ROI: 2.0
  - evidence: packages/cli/src/commands/uninstall.ts:52,62,72,80 — yellow ✗ logged but looks like success
  - approach: Change to dim/grey output or return early with informational message only when --verbose.

- [ ] Validate fetched skill manifest against schema in install command
  - category: security | severity: medium | effort: m | ROI: 0.67
  - evidence: packages/cli/src/commands/install.ts:49 — manifest fetched without schema validation
  - approach: Load and compile skill.schema.json; validate manifest JSON before using it.

- [ ] Add catalog data integration assertions to Playwright suite
  - category: test-gap | severity: medium | effort: m | ROI: 0.67
  - evidence: apps/web/tests/catalog.spec.ts — 13 tests are navigation-only
  - approach: Add assertions for entry count > 0, h2 category sections visible, install command text rendered on detail pages.

- [ ] Add error handling for JSON parsing in CLI catalog index loading
  - category: bug | severity: medium | effort: m | ROI: 0.67
  - evidence: packages/cli/src/lib/catalog.ts — readCachedIndex/readLocalIndex JSON.parse unguarded
  - approach: Wrap JSON.parse calls in try-catch; log warning and continue to next fallback on parse error.

- [ ] Add error handling for YAML parsing in web catalog loaders
  - category: bug | severity: medium | effort: s | ROI: 1.0
  - evidence: apps/web/src/lib/catalog.ts — yaml.load calls without try-catch
  - approach: Wrap yaml.load in try-catch; log malformed file path and skip entry rather than crashing build.

- [ ] Add file read error handling in skill/hook/command/tool catalog loaders
  - category: bug | severity: medium | effort: m | ROI: 0.67
  - evidence: packages/catalog/scripts/lib/catalog.ts — readdir/stat without error handling
  - approach: Add try-catch around readdir and readFile; skip inaccessible entries and log warning.

- [ ] Add file read error handling in doc/agent/tutorial catalog loaders
  - category: bug | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/catalog/scripts/lib/catalog.ts — doc/agent/tutorial loaders mirror above pattern
  - approach: Same as above — try-catch; skip + warn on read failure.

- [ ] Add post-parse field validation in web catalog loaders
  - category: bug | severity: medium | effort: m | ROI: 0.67
  - evidence: apps/web/src/lib/catalog.ts — no validation of required fields after JSON.parse
  - approach: After parse, check id, name/title, description fields present; filter out and warn on invalid entries.

- [ ] Add duplicate item detection in collection validation
  - category: test-gap | severity: medium | effort: m | ROI: 0.67
  - evidence: packages/catalog/scripts/validate-catalog.ts — referential check exists but no dup detection
  - approach: For each collection, check for duplicate (kind, id) pairs in items array; fail validation if found.

- [ ] Validate GATEWAY_PORT within valid range (1-65535)
  - category: security | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/cli/src/lib/gateway.ts:17 — Number() conversion with no bounds check
  - approach: After Number() conversion, assert port > 0 && port <= 65535; throw user-friendly error if invalid.

- [ ] Add fetch timeout (AbortSignal) to remote catalog fetch
  - category: performance | severity: medium | effort: m | ROI: 0.67
  - evidence: packages/cli/src/lib/catalog.ts:fetchRemoteIndex — no timeout; can hang indefinitely
  - approach: Pass `signal: AbortSignal.timeout(10_000)` to fetch; catch AbortError and fall through to cache.

- [ ] Fix naive .env parsing in gateway lib (unhandled quoted values)
  - category: bug | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/cli/src/lib/gateway.ts:27 — simple split fails on quoted values with spaces
  - approach: Replace manual split with proper .env line parser handling quotes, escapes, and comments.

- [ ] Add TOML parse error boundary in editors.ts
  - category: bug | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/cli/src/lib/editors.ts:125 — TOML.parse without try-catch in install path
  - approach: Wrap TOML.parse in try-catch; surface user-friendly "corrupted Codex config" error with path.

- [ ] Add npm dependency audit to CI validate workflow
  - category: security | severity: medium | effort: s | ROI: 1.0
  - evidence: .github/workflows/validate.yml — no dep vulnerability scan (only semgrep for code)
  - approach: Add `npm audit --audit-level=high` job to validate.yml; allow-list known false positives.

- [ ] Add i18n field completeness assertions in catalog validation
  - category: test-gap | severity: medium | effort: s | ROI: 1.0
  - evidence: packages/catalog/scripts/validate-catalog.ts — validates presence of pt-BR block but not all required fields
  - approach: For each entry kind, assert all required translation fields (name/title, description) are non-empty strings in pt-BR block.

## Run Summary

- Discovered: 25 raw findings
- Skipped (existing issues): 5 (pt-BR locale gap #266, tutorial content #144, collection split #265, deploy-web decision #218, dev-assets-import #150)
- Approved: 25 (all)
- Critic: 0 duplicates, 3 splits applied, 7 severity corrections, 2 new classes added
- Features for spec: 1 (enforce locale parity in CI)
