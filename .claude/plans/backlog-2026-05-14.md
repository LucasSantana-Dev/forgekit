# Backlog Plan — forgekit (2026-05-14)

_Repo: ai-dev-toolkit (forgekit) · Branch: main · HEAD: 3145a7a · Generated: 2026-05-14_
_Supersedes: `backlog-2026-05-06.md` (v0.17.0 era; 6 releases shipped since)_

## Goal

Land v0.24.0, clear the high-priority bug + draft-release tail, then sequence
the provider-taxonomy and tutorials/i18n issue clusters (27 open) into shippable
slices without growing scope.

## In scope

- Open PR #182 (v0.24.0) — merge and tag.
- Open issues #137–#163 grouped into 4 clusters (priority bugs, providers,
  tutorials/i18n, catalog content).
- Stale release artifact (v0.18.0 Draft).
- Tier 3 catalog promotion candidates flagged in 2026-05-13 handoff.
- Working-tree state (7 untracked + 3 modified kit/core/skills) — already
  captured in PR #182; no separate action.

## Out of scope

- Content strategy / editorial backlog (separate `/content-strategy` session,
  needs intake answers first).
- Plugin system (#148) — explicitly deferred until core surface stabilizes.
- `dev-assets-import` CLI feature (#150) — XL effort, low priority, defer.
- Forge-Space references (org wound down).
- Cloudflare Worker rename (`ai-dev-toolkit-library` intentionally retained).

## Current state (verified evidence)

| Signal | Value |
|---|---|
| Latest release | v0.23.0 (2026-05-13) |
| Open PRs | 1 — #182 v0.24.0, mergeStateStatus=`BEHIND`, all CI green |
| Open issues | 27 (2 high priority, 17 medium, 8 low) |
| High-priority bugs | #145 (CI pins non-existent action versions), #138 (pt-BR tutorials 404) |
| Draft release | v0.18.0 (stale since 2026-05-06) |
| Working tree | 3M + 7?? in `packages/core/kit/core/skills/` — sources for PR #182 |
| Catalog state | 149 skills, 21 servers, 22 docs, 11 tools, 24 collections, 291 web pages (post v0.23.0) |

## Reconciliation vs 2026-05-06 backlog

| 2026-05-06 item | Status |
|---|---|
| A1 wrangler PR #124 | ✅ shipped (#170) |
| A2 Astro 5→6 PR #123 | ✅ shipped (#123, see memory) |
| A3 marked 14→18 PR #122 | ✅ shipped |
| A4 Dependabot alerts | ⚠ unverified — re-check in Phase 2 |
| A5 v0.18.0 release | ⚠ Draft only — see Phase 2 |
| B1 forgekit rename (185 files) | ⚠ open as issue #149 (~109 refs remain) |
| B2 curation.ts → YAML | ⏳ not started, deferred |
| B3 validate-secrets hook | ✅ resolved silently |
| B4 pt-BR translation | ✅ shipped in batch releases |
| B5 collections sort/filter | ⏳ not started, deferred |
| D1–D3 tech debt | ⏳ not started, low impact, deferred |
| I1–I3 docs | ⏳ stale, low impact, deferred |
| P (providers content) | ⏳ open as issues #154–#163, see Phase 3 |

---

## Phase 1 — Land v0.24.0 (today)

**Scope:** Single PR (#182), already CI-green, just needs a rebase.

1. Rebase `feat/v0.24.0` onto `main` (currently `BEHIND` after #181 + #183 landed).
2. Re-verify CI on the rebase head.
3. Squash-merge via `/merge-confidently`.
4. Confirm Release workflow auto-tags v0.24.0.

**Validation:**
- `gh pr view 182 --json mergeStateStatus` → `CLEAN`
- `gh release list | grep v0.24.0` → `Latest`
- `pnpm catalog:validate` clean on main post-merge

**Replan trigger:** CI red on rebase head (rare — caused by `catalog:llms` drift integration from #183).

---

## Phase 2 — Clear stale release + high-priority bugs (this week)

**Scope:** Two issues + one stale draft. Independent, parallelizable.

### 2a. Fix #145 — CI workflows pin non-existent `actions/checkout@v6` / `actions/setup-node@v6`
- `priority: high`, `effort: xs`.
- Grep `.github/workflows/` for the bad pins; flip to `@v5`.
- **Validation:** CI green on the fix PR; no "unknown ref" warnings in GH Actions logs.

### 2b. Fix #138 — pt-BR tutorial nav links → 404
- `priority: high`, `effort: s` (~2–4h).
- Either build the pt-BR tutorial pages or remove the dead nav links.
- **Validation:** `pnpm web:build` clean; manual check of `/pt-br/tutoriais/` (or whatever the canonical pt-BR path is).
- **Dependency:** look at the broader pt-BR tutorial story (#137, #139, #140, #141) before deciding — fix may cascade into Phase 4. If so, scope down 2b to "remove dead links only" and roll the real fix into Phase 4.

### 2c. Resolve v0.18.0 Draft release
- Verify whether v0.18.0 was actually cut between v0.17.0 and v0.19.0 (memory says no; release list shows it as Draft).
- If shipped under a different number → delete the draft.
- If never shipped → delete (v0.19.0 superseded it).
- **Validation:** `gh release list` shows no Draft entries.

**Replan trigger:** #138 turns out to require building the full pt-BR tutorial pipeline → roll into Phase 4 and re-plan Phase 2.

---

## Phase 3 — Provider taxonomy cluster (next 1–2 weeks)

**Scope:** 10 issues (#154–#163). Sequencing already documented in
`providers-page-content.md`. Schema → backfill → seeds → polish.

1. **#154** — add `provider` field to manifest schema (M, medium).
2. **#155** — backfill provider across existing entries (L, medium). Largest item; can ship a script.
3. **#156, #157, #158, #159** — seed Codex/Gemini/Cursor/Local buckets in parallel after #155 lands (each M, low).
4. **#160 + #161 + #163** — polish phase (blurbs + deep pages + badges).
5. **#162** — `PROVIDERS.md` doc (independent, ship anytime after #154).

**Validation per slice:** `pnpm catalog:validate` clean; `/providers/` page renders non-empty buckets for each seeded provider.

**Replan trigger:** schema design (#154) reveals a backwards-incompatible
change → split into v0.25.0-major or feature-flag the field.

---

## Phase 4 — Tutorials + i18n parity cluster (next 1–2 weeks)

**Scope:** 6 issues (#137, #138-or-residual, #139, #140, #141, #144).

1. **#137** — include tutorials in search index (S).
2. **#140** — EN homepage hero stats + total count include tutorials (xs).
3. **#138** — pt-BR tutorial pages built (if scoped here, see Phase 2b).
4. **#139** — RelatedTutorials component added to pt-BR detail pages (S).
5. **#141** — EN/pt-BR homepage feature parity (M).
6. **#144** — content gap: only 1 tutorial in catalog (XL, low) → write 2–3 tutorials in pillars TBD.

**Validation per slice:** locale-by-locale visual diff; `pnpm web:build` clean;
`/tutorials/` and `/pt-br/tutoriais/` both render expected page count.

**Replan trigger:** content gap (#144) requires the `/content-strategy` skill
to define which tutorials to write → block #144 on that flow.

---

## Phase 5 — Catalog content + housekeeping (rolling)

**Scope:** Lower-priority issues; ship opportunistically alongside other work.

| Issue | Why | When |
|---|---|---|
| #149 | finish pt-BR locale rename (109 refs remain) | one-shot sed PR |
| #143 | 61 skills not in any collection — coverage gap | as collections grow |
| #142 | commands catalog directory missing | when first command lands |
| #146 | professional-work-toolkit ↔ aws-kiro-ide-workflow overlap (~9 items) | resolve with editorial pass |
| #147 | llms.txt stale | ✅ already resolved by #181 + #183 (close the issue) |
| #152, #153 | collection detail page + membership exposed in index | feature work, queue after Phase 3 |
| #151 | AWS Kiro collection — partially shipped | finish curation |

**Validation:** each lands as its own PR with `pnpm workspace:validate` clean.

---

## Phase 6 — Tier 3 batch (v0.25.0 candidate, opportunistic)

**Scope:** Stack-specific suites flagged in 2026-05-13 handoff.

- **firecrawl (7 skills):** firecrawl, firecrawl-agent, firecrawl-crawl,
  firecrawl-download, firecrawl-instruct, firecrawl-map, firecrawl-search.
- **huggingface (7+ skills):** huggingface-best, -community-evals, -datasets,
  -gradio, -llm-trainer, -local-models, -paper-publisher, -papers, -vision-trainer.

**⚠ Pre-work:** explore agent flagged thin SKILL.md descriptions (`>`/`|`
placeholder bodies). Audit each before batch promotion — likely needs rewrites,
not just copies. Reuse the proven workflow from
`project_batch_skill_promote_workflow.md` memory.

**Validation:** every skill has a non-placeholder body, ≥1 collection
reference, full pt-BR translation, `pnpm workspace:validate` clean.

**Replan trigger:** placeholder rewrites exceed batch budget → split firecrawl
and huggingface into separate releases.

---

## Phase 7 — Tech debt (deferred, rolling)

Lifted from 2026-05-06 backlog, still not load-bearing:

- D1 `i18n.ts` split (397 lines, 7 churn).
- D2 `catalog.ts` per-type split (357 lines, 7 churn).
- D3 schema-loader extraction.
- B2 `curation.ts` → YAML (8 churn).
- E2 `validate.yml` → reusable workflow.

**When to act:** ship one of these as a "Friday refactor" after any phase that
churns the same file; otherwise leave.

---

## Out of scope / won't fix (carried forward)

| Item | Reason |
|---|---|
| Plugin system (#148) | Deferred until core surface stable; XL effort |
| `dev-assets-import` (#150) | XL effort, low priority |
| Forge-Space integration | Org wound down |
| Cloudflare Worker rename | Intentionally retained (`feedback_no_claude_coauthor.md`) |
| E2E web tests | Static site, 1.22s build — not a pain point |
| Codex parity re-verification | Out of repo; runs in Codex session, blocked by usage cap |

---

## Top 10 by impact/effort ratio

| # | Item | Phase | Impact | Effort |
|---|---|---|---|---|
| 1 | Merge PR #182 (v0.24.0) | 1 | high | xs |
| 2 | Fix #145 CI action pins | 2a | high | xs |
| 3 | Resolve v0.18.0 Draft | 2c | low | xs |
| 4 | Close #147 (llms.txt — already fixed) | 5 | low | xs |
| 5 | Fix #138 pt-BR tutorial nav | 2b | high | s |
| 6 | #154 manifest `provider` field | 3 | med | m |
| 7 | #137 tutorials in search | 4 | med | s |
| 8 | #149 finish pt-BR rename (109 refs) | 5 | med | m |
| 9 | #155 backfill provider | 3 | med | l |
| 10 | Tier 3 firecrawl batch | 6 | med | m (after audit) |

## Unblocks map

```
Phase 1 (v0.24.0)
  ↓
Phase 2a/2b/2c (independent, parallel)
  ↓
Phase 3 (#154 → #155 → seeds → polish)        Phase 4 (tutorials/i18n)
  ↓                                             ↓
Phase 5 (rolling housekeeping; can run alongside 3+4)
  ↓
Phase 6 (Tier 3 v0.25.0 — opportunistic)
```

## Replanning triggers (global)

- New batch of friction-driven skills surfaces (current PR #182 came from one
  session's frictions) — pull forward as v0.X.0 PR, defer later phases.
- High-severity CVE on a catalog dependency → preempt with security release.
- Content-strategy intake completes → Phase 4 #144 unblocks.
- Composite-router hook emits `🎯 Composite match: /<name>` mid-phase → defer
  to the composite, don't shadow it with this plan.

## Resume hooks

- Open handoff: `~/.claude/handoffs/ai-dev-toolkit/latest.md` (4-release batch, 2026-05-13)
- Memories:
  - `project_batch_skill_promote_workflow.md` — full 14-step batch recipe
  - `project_skill_md_yaml_strict_parser.md` — YAML quoting gotcha
  - `feedback_forgekit_branch_protection.md` — main is protected; PR-only path
  - `feedback_parallel_session_release_race.md` — check competing releases before Phase 1
- Prior plans referenced:
  - `providers-page-content.md` (canonical sequencing for Phase 3)
  - `catalog-collections-impl-spec.md` (Phase 5 background)
  - `homepage-ui-redesign.md` (Phase 4 i18n background)
