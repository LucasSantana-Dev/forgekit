# Backlog Map — 2026-05-06

_Repo: ai-dev-toolkit (forgekit) · Branch: main · HEAD: b867d11 · Generated: 2026-05-06_

## Snapshot

| Signal | Value |
|--------|-------|
| Latest release | v0.17.0 (2026-04-18, 18 days ago) |
| Commits since release | ~10 (catalog cleanup, collection add, web improvements) |
| Open PRs | 3 (all Dependabot) |
| Open issues | 0 |
| CI on HEAD | ✅ All checks passing (validate, lint, tests, TruffleHog, Semgrep) |
| Catalog | 94 skills, 13 collections, 10 servers, 15 agents, 26 hooks |
| Local skills installed | 86 (pruned 60 unused today) |

## Prior Art Reconciliation (vs 2026-04-25 backlog)

| Prior Item | Status |
|-----------|--------|
| #1 Astro 5→6 hygiene | PR #123 open, CI fully green, **MERGEABLE** → promoted to A2 |
| #2 ai-dev-toolkit → forgekit rename (~200 refs) | PR #121 shipped but **185 refs remain** in locales/pt-BR/patterns + kit → still open, B2 |
| #3 package.json URLs | ✅ Shipped (PRs #117, #119) |
| #4 forge-space-knowledge-import Phase A | ❌ Forge-Space org wound down (commit #88) → J (won't fix) |
| #5 Triage orphaned branches | ✅ Done |
| #6 dev-assets hardcoded paths | ✅ Shipped (PR #97) |
| #7 GH Actions Node-24 bump | ✅ Shipped (PR #118) |
| G2 Enable Dependabot alerts | ❌ Still disabled → A4 |

---

## A. Now (this week, ≤1 PR each)

### A1. Merge wrangler PR #124 (patch bump 4.85.0 → 4.88.0)
_Why_: Patch release, all 14 CI checks green, MERGEABLE. Zero risk.  
_Evidence_: `gh pr view 124` — GitGuardian, TruffleHog, Validate catalog: all SUCCESS  
_PR_: #124  
_Effort_: S · _Blocker_: none  
`[impact: low] [effort: S] [risk: low] [confidence: high]`

### A2. Merge Astro PR #123 (5.18.1 → 6.2.2)
_Why_: All 14 CI checks pass (Semgrep marked SKIPPED — not a blocker). MERGEABLE. Prior backlog item #1 was tracking the GHSA-j687-52p2-xcff hygiene bump; build and tests now confirm 6.x is compatible with this codebase.  
_Evidence_: `gh pr view 123` — Validate catalog/CLI/web: SUCCESS; Run unit tests: SUCCESS. Static Astro output + no `define:vars` usage = not exploitable.  
_PR_: #123  
_Effort_: S · _Blocker_: none (review Astro 6 changelog for any output-format changes before merging)  
`[impact: med] [effort: S] [risk: med] [confidence: high]`

### A3. Merge marked PR #122 (14.1.4 → 18.0.3)
_Why_: 4 major versions behind on a Markdown renderer (security surface). All 14 CI checks pass — unit tests confirm no rendering regressions in this codebase. MERGEABLE.  
_Evidence_: `gh pr view 122` — Run unit tests: SUCCESS; Check Markdown Links: SUCCESS  
_PR_: #122  
_Effort_: S · _Blocker_: none  
`[impact: med] [effort: S] [risk: low] [confidence: high]`

### A4. Enable Dependabot alerts on forgekit
_Why_: `gh api /repos/LucasSantana-Dev/forgekit/dependabot/alerts` returns HTTP 403. Alerts are disabled, meaning CVEs on dependencies produce no signal until npm audit or manual check. Two minutes in GitHub Settings → Code security.  
_Evidence_: Command output: `{"message":"Dependabot alerts are disabled","status":"403"}`  
_PR_: none (Settings UI change)  
_Effort_: S · _Blocker_: none  
`[impact: high] [effort: S] [risk: low] [confidence: high]`

### A5. Cut v0.18.0
_Why_: 18 days since v0.17.0. Today's work (6 catalog entries pruned, professional-work-toolkit collection, web nav reorder + collections sort) warrants a release bump. After merging A1–A3 Dependabot PRs, the next meaningful tag becomes v0.18.0.  
_Evidence_: `gh release list` — latest is `v0.17.0` at `2026-04-18T21:34Z`; HEAD is 10+ commits ahead  
_PR_: new (CHANGELOG promotion + tag)  
_Effort_: S · _Blocker_: merge A1–A3 first  
`[impact: med] [effort: S] [risk: low] [confidence: high]`

---

## B. Next (next 2 weeks)

### B1. Complete ai-dev-toolkit → forgekit rename in locales/pt-BR and kit/
_Why_: 185 files still reference `ai-dev-toolkit` — predominantly in `locales/pt-BR/patterns/` and `locales/pt-BR/kit/core/skills/`. PR #121 was supposed to finish this but clearly only covered part of the surface.  
_Evidence_: `grep -r "ai-dev-toolkit" ... | wc -l` → 185 files. Prior backlog item #2 estimated ~200 refs; 185 remain.  
_Next step_: `grep -rl "ai-dev-toolkit" locales/ kit/ | xargs sed -i '' 's/ai-dev-toolkit/forgekit/g'` (verify no false positives first)  
_Effort_: M · _Blocker_: none (but high file count → create a focused PR)  
`[impact: med] [effort: M] [risk: low] [confidence: high]`

### B2. Externalize homepage curation config from `curation.ts` to YAML
_Why_: `apps/web/src/lib/curation.ts` (222 lines) hardcodes `FEATURED_IDS` and `WORKFLOW_THEMES`. It's the 11th-most-churned file in 90 days (8 hits). Every featured item rotation = code commit + redeploy.  
_Evidence_: `git log --since="90 days ago" -- apps/web/src/lib/curation.ts` → 8 commits; `wc -l curation.ts` → 222  
_Next step_: Create `apps/web/src/data/homepage-curation.yaml`; load via existing `js-yaml` dep; remove hardcoded maps from `curation.ts`  
_Effort_: M · _Blocker_: none  
`[impact: med] [effort: M] [risk: low] [confidence: high]`

### B3. Add `validate-secrets` hook catalog entry
_Why_: `security-first-dev.yaml` collection references `kind: hook, id: validate-secrets` but the catalog has no `hooks/validate-secrets/manifest.json`. Running `pnpm --filter catalog validate` passes (likely soft-ref), but the collection detail page would fail to render the hook card.  
_Evidence_: `ls packages/catalog/catalog/hooks/` — `validate-secrets` dir exists with `manifest.json` + `script.sh` (confirmed by haiku agent). Re-run catalog validation to confirm it resolves.  
_Next step_: Confirm with `pnpm --filter catalog validate` after A5; if warning surfaces, add proper manifest.  
_Effort_: S · _Blocker_: A5  
`[impact: low] [effort: S] [risk: low] [confidence: med]`

### B4. pt-BR translation for `professional-work-toolkit` collection
_Why_: Collection was created today with a `translations.pt-BR` block, but the description is a manual translation — needs review for naturalness and consistency with existing pt-BR catalog voice.  
_Evidence_: `packages/catalog/catalog/collections/professional-work-toolkit.yaml` — pt-BR block present but unreviewed  
_Next step_: Compare wording against `security-first-dev.yaml` pt-BR as style reference; submit as a single-file PR  
_Effort_: S · _Blocker_: none  
`[impact: low] [effort: S] [risk: low] [confidence: high]`

### B5. Add sort/filter controls to collections index page
_Why_: Collections page now sorts by item count (shipped today), but there's no UI affordance for users to re-sort by name or filter by tag. With 13 collections the list is manageable; at 20+ it becomes unwieldy.  
_Evidence_: `apps/web/src/pages/collections/index.astro` — static sort only, no tag filter; `CollectionsFeaturedGrid.astro:26` — no controls  
_Next step_: Add tag-filter pills above the grid (client-side JS, reuse existing `data-tags` attribute on Card); add sort toggle (name/size)  
_Effort_: M · _Blocker_: none  
`[impact: med] [effort: M] [risk: low] [confidence: high]`

---

## C. Bug Fixes / Incidents

None. All CI checks green. Zero open GitHub issues. Zero npm audit vulnerabilities.

---

## D. Tech Debt

### D1. `apps/web/src/lib/i18n.ts` — 397-line monolithic translation file
_What breaks if not fixed_: As catalog categories grow (new skill types, new UI sections), this file grows unboundedly. 7 churn hits in 90 days already. Adding a new locale key requires editing a 397-line file — conflict risk in parallel PRs.  
_Evidence_: `wc -l apps/web/src/lib/i18n.ts` → 397; `git log --since="90 days ago" -- apps/web/src/lib/i18n.ts | wc -l` → 7 commits  
_Fix_: Split by namespace (`nav.ts`, `home.ts`, `list.ts`, `usage.ts`, `install.ts`) into `src/lib/i18n/`  
`[impact: low] [effort: M] [risk: med] [confidence: med]`

### D2. `apps/web/src/lib/catalog.ts` — 357 lines, no per-type module split
_What breaks if not fixed_: All catalog types (skills, agents, servers, hooks, collections, tools, commands, docs) load through a single file. Adding a new catalog type (e.g., `prompts`) requires touching a 357-line file that every page imports.  
_Evidence_: `wc -l apps/web/src/lib/catalog.ts` → 357; churn in 90d: 7 commits  
_Fix_: Split into `catalog/{skills,agents,servers,hooks,collections,tools,commands}.ts` with shared `loader.ts` base  
`[impact: low] [effort: M] [risk: med] [confidence: med]`

### D3. `scripts/validate-schemas.js` duplicates schema loading with `test/validate-schemas.test.js`
_What breaks if not fixed_: Schema loading logic diverges over time — validator script and test suite have separate implementations; a schema change requires two edits.  
_Evidence_: Both files in top-10 churn (10 hits each in 90 days) — `git log --since="90 days ago" --name-only ... | sort | uniq -c | sort -rn | head -25`  
_Fix_: Extract shared `packages/catalog-scripts/src/schema-loader.ts` used by both  
`[impact: low] [effort: M] [risk: low] [confidence: med]`

---

## E. Refactor Opportunities

### E1. Consolidate `curation.ts` into YAML config (see B2)
Structural change: removes code-as-config pattern, enables editorial updates without deploys. Tied to D1 churn data. Unlocks B5 (sort/filter) more cleanly.

### E2. `validate.yml` CI churn (13 hits in 90 days) → extract reusable workflow
_Evidence_: `.github/workflows/validate.yml` is the 5th-most-churned file (13 hits). It's a monolithic workflow with 10+ jobs. Extracting reusable workflows (`workflow_call`) for catalog-validate, web-build, and security jobs would reduce per-PR CI surface.  
_Unlocks_: Faster CI iteration — separate jobs can be re-run without re-running unrelated checks.  
`[impact: low] [effort: M] [risk: low] [confidence: med]`

---

## F. Performance / Optimization

### F1. Astro 6 build pipeline (tracked under A2/H1)
Astro 6 ships Vite 6 internals and improved partial hydration. Current build: 398 pages in 1.22s — already fast. Post-upgrade, run `time pnpm --filter web build` before/after to confirm regression-free.  
_Baseline needed_: `time pnpm --filter web build` on current HEAD before merging PR #123  
`[impact: low] [effort: S] [risk: low] [confidence: med]`

### F2. Catalog `index.json` (189 entries) fully loaded at build time on every page
_Why it matters_: `src/lib/catalog.ts:getSkills()` reads and parses `index.json` on every static page render. At 189 entries + projected growth to 300+, this stays fast for static builds but would be slow if catalog goes SSR. Needs no action now — flag for review if SSR is ever added.  
_Baseline_: Needs baseline measurement  
`[impact: low] [effort: L] [risk: low] [confidence: low]`

---

## G. Security

### G1. Enable Dependabot alerts (see A4)
`[impact: high] [effort: S] [risk: low] [confidence: high]`  
_Current state_: HTTP 403 on alerts API — disabled. No CVE early-warning system in place.

### G2. marked 14→18: 4 major versions on a rendering library (see A3)
_Advisory_: No open CVEs found via `npm audit --json` (0 vulnerabilities). However, Markdown renderers are a common XSS surface. 4 major version jumps without known CVEs = unknown risk surface until audited.  
_Mitigation_: Merge A3 (all tests pass). Post-merge, review marked 15/16/17/18 changelogs for security-relevant changes.  
`[impact: med] [effort: S] [risk: low] [confidence: high]`

### G3. Dependabot alerts disabled → no CVE signal on future dependencies
_What breaks if not fixed_: A new dependency added to the catalog (e.g., a new server manifest that references a vulnerable npm package) produces no alert. The only safety net is `npm audit` on CI, which only checks direct/transitive deps of this monorepo, not catalog-referenced packages.  
_Fix_: Settings → Code security → Enable Dependabot alerts + Dependabot security updates  
`[impact: high] [effort: S] [risk: low] [confidence: high]`

---

## H. Major-Version Migrations (deferred)

### H1. astro 5→6 (PR #123)
_Migration cost_: Low — PR opened by Dependabot, CI fully green, no TypeScript errors, no rendering test failures. The audit noted GHSA-j687-52p2-xcff is not exploitable here. **Reclassified from deferred to A2** — ready to merge this week.  
_CVE risk of staying_: GHSA-j687-52p2-xcff (moderate, not exploitable in static output mode)  
`[impact: med] [effort: S] [risk: med] [confidence: high]`

### H2. marked 14→18 (PR #122)
_Migration cost_: Low — all CI checks pass. 4 major versions (15, 16, 17, 18) historically include output format changes for edge-case Markdown. The catalog uses `marked` to render skill descriptions; no rendering regression detected by test suite.  
_CVE risk of staying_: Unknown — 4 major versions unreviewed  
**Reclassified to A3** — ready to merge.  
`[impact: med] [effort: S] [risk: low] [confidence: high]`

---

## I. Documentation

### I1. BACKLOG.md is 18 days stale
_Evidence_: `git log --format="%ai" -- BACKLOG.md | head -1` → `2026-04-18`. Doesn't reflect today's catalog cleanup, new collection, or web improvements.  
_Fix_: Update `BACKLOG.md` to reflect v0.17.0 shipped items and new Now/Next from this audit.  
`[impact: low] [effort: S] [risk: low] [confidence: high]`

### I2. `docs/roadmap.md` shows `2026-04-22-toolkit-monorepo-rebrand` as "active"
_Evidence_: `docs/roadmap.md` (last regenerated 2026-04-22). Most monorepo rebrand tasks shipped (PRs #117, #119, #121) but the spec is not marked shipped and roadmap not regenerated.  
_Fix_: Mark spec status as `shipped` → regenerate `docs/roadmap.md`  
`[impact: low] [effort: S] [risk: low] [confidence: high]`

### I3. No `CONTRIBUTING.md` at repo root
_Evidence_: `ls /Volumes/External HD/Desenvolvimento/ai-dev-toolkit/` — no CONTRIBUTING.md found. The `docs/guides/` directory exists but no contributor guide.  
_Fix_: Add `CONTRIBUTING.md` with: dev setup, how to add a catalog entry, how to add a collection, PR checklist  
`[impact: low] [effort: S] [risk: low] [confidence: high]`

---

## J. Out of Scope / Won't Fix

| Item | Reason |
|------|--------|
| `forge-space-knowledge-import` Phase A | Forge-Space org wound down (commit #88 `chore: remove Forge-Space references`); spec is moot |
| Cloudflare Worker name (`ai-dev-toolkit-library`) | Intentionally not renamed per memory: `feedback_no_claude_coauthor.md` + `wrangler.jsonc:13-14` |
| Plugin system (`kit/plugins/`) | Per BACKLOG.md: deferred until core surface stabilizes |
| Company templates beyond fullstack-forge | Per BACKLOG.md: deferred |
| E2E web tests | No framework, greenfield scope; static site with 1.22s build — not a pain point yet |
| Dependabot alerts API auth for programmatic checks | 403 is expected without admin token; enable via UI (A4) |
| `forge-space-resume` local skill | Already removed from local skills today; catalog has no corresponding entry |

---

## Top 10 by Impact/Effort Ratio

| # | Item | Impact | Effort | Risk | Confidence |
|---|------|--------|--------|------|------------|
| 1 | A4: Enable Dependabot alerts | high | S | low | high |
| 2 | A1: Merge wrangler PR #124 | low | S | low | high |
| 3 | A3: Merge marked PR #122 | med | S | low | high |
| 4 | A2: Merge Astro PR #123 | med | S | med | high |
| 5 | A5: Cut v0.18.0 | med | S | low | high |
| 6 | B1: Complete forgekit rename (185 files) | med | M | low | high |
| 7 | B2: Externalize curation.ts to YAML | med | M | low | high |
| 8 | B5: Add sort/filter to collections page | med | M | low | high |
| 9 | I1: Update stale BACKLOG.md | low | S | low | high |
| 10 | I2: Close monorepo-rebrand spec as shipped | low | S | low | high |

## Unblocks Map

```
A1 (wrangler) ─┐
A2 (astro)    ─┤─→ A5 (v0.18.0 release)
A3 (marked)   ─┘

A4 (Dependabot) → G3 resolved (future CVE coverage)

B2 (curation.ts → YAML) → B5 (sort/filter controls — cleaner implementation)

B1 (rename 185 files) → I2 (roadmap regeneration — clean state)
```

---

## P. Providers page — content backlog (added 2026-05-08)

The new `/providers/` page (Skills/Agents/Hooks/Commands/Tools/MCP grouped by primary AI provider) ships with the schema-derivation fallback in `apps/web/src/lib/ui.ts → deriveProvider()`. Without explicit provider tags every entry currently buckets to **Claude**, leaving Codex / Gemini / Cursor / Local sections empty. The full plan and dependency graph live in `.claude/plans/providers-page-content.md`. Issues opened today:

| # | Issue | Phase | Effort | Priority |
|---|-------|-------|--------|----------|
| 1 | [#154](https://github.com/LucasSantana-Dev/forgekit/issues/154) feat(catalog): add `provider` field to manifest schema | Schema | M | medium |
| 2 | [#155](https://github.com/LucasSantana-Dev/forgekit/issues/155) feat(catalog): backfill `provider` across existing entries | Backfill | L | medium |
| 3 | [#156](https://github.com/LucasSantana-Dev/forgekit/issues/156) feat(catalog): seed Codex bucket (≥3 entries) | Seed | M | low |
| 4 | [#157](https://github.com/LucasSantana-Dev/forgekit/issues/157) feat(catalog): seed Gemini bucket (≥3 entries) | Seed | M | low |
| 5 | [#158](https://github.com/LucasSantana-Dev/forgekit/issues/158) feat(catalog): seed Cursor bucket (≥3 entries) | Seed | M | low |
| 6 | [#159](https://github.com/LucasSantana-Dev/forgekit/issues/159) feat(catalog): seed Local LLM bucket (≥3 entries) | Seed | M | low |
| 7 | [#160](https://github.com/LucasSantana-Dev/forgekit/issues/160) feat(web): editorial blurb per provider section | Polish | S | low |
| 8 | [#161](https://github.com/LucasSantana-Dev/forgekit/issues/161) feat(web): per-provider deep page `/providers/[id]/` | Polish | M | low |
| 9 | [#162](https://github.com/LucasSantana-Dev/forgekit/issues/162) docs: PROVIDERS.md taxonomy + install path conventions | Docs | S | low |
| 10 | [#163](https://github.com/LucasSantana-Dev/forgekit/issues/163) feat(web): provider badge on Card.astro and EntryHeader | UX | S | low |

### Sequencing

```
#154 (schema) ─→ #155 (backfill) ─┬─→ #156 (Codex seed)
                                  ├─→ #157 (Gemini seed)
                                  ├─→ #158 (Cursor seed)
                                  └─→ #159 (Local seed)
                                            └─→ #160 (blurbs) ─→ #161 (deep pages) ─→ #163 (badges)

#162 (PROVIDERS.md) — independent, ship anytime after #154
```

### When to start

Block on the current branch's PR landing (which introduces the page itself). Then ship #154 + #155 in a single sprint. The four seed issues (#156–#159) are parallelisable across contributors. #160 + #161 + #163 land last as polish.

### Won't-fix / explicitly deferred

- Provider-specific install commands (`~/.codex/`, `~/.cursor/rules/`, etc) beyond the `npx forge-kit install` flow — captured in #162 docs only; runtime change is its own future spec.
- Auth / API-key onboarding flows for non-Claude providers — out of scope for this catalog.
