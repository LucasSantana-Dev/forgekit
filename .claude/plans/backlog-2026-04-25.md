# Backlog map — forgekit (formerly ai-dev-toolkit) — 2026-04-25

**Scope**: single repo at `/Volumes/External HD/Desenvolvimento/ai-dev-toolkit`. Remote renamed today: `LucasSantana-Dev/forgekit`. Default branch `main` clean and in sync. HEAD 12 h old. Latest tag `v0.17.0` (7 d). Open PRs: 0. Open issues: 0.

## Status update — 2026-04-25 autonomous execution

Shipped this session:
- ✅ #3 — root + `packages/{catalog,cli}` `repository.url` → `forgekit` (PR #117, commit `88a232d`)
- ✅ #5 — 3 orphan remote branches deleted (content already on main via PRs #93/#94/#95 + monorepo path move in PR #96)
- ✅ #7 — GH Actions Node 24 bump (PR #118, commit `68bc2ac`); post-merge Deploy webapp run 24934616028 confirmed warning gone, 45 s, all green
- ✅ #9 — `import-library` git remote removed locally
- ✅ #10 — stale handoff archived
- ✅ pt-BR locale `repository.url` → `forgekit` (PR #119, commit `fd60526`); was pointing at the wound-down `Forge-Space` org
- ✅ D5 — 42 stale remote branches deleted (verified all squash-merged via `git cherry` + `gh pr list --state merged --head <b>`); 33 corresponding local branches + 3 orphan worktrees cleaned. Origin now down to `main`, `personal`, `release/v0.15.0`, `release/v0.16.0`.
- ✅ Top-10 spec stub committed at `docs/specs/2026-04-25-backlog-map/spec.md` (PR #120, commit `c57acff`)
- ✅ #2 — internal-references rename `ai-dev-toolkit` → `forgekit` (PR #121, commit `0e191fd`); 109 files, 196 line edits, all mechanical URL/cd-command updates per the rebrand playbook § 4. Post-merge Deploy webapp run 24941504293 verified: 38 s, all green, smoke check 200.

Dropped on closer inspection:
- ❌ #8 — `deploy-web.yml` comments referring to `ai-dev-toolkit-library`. The Cloudflare Worker is still deliberately named `ai-dev-toolkit-library` per `apps/web/wrangler.jsonc:13-14`. Comments are accurate. Will revisit when the CF app rename happens.

Process learnings:
- ⚠️  `gh pr merge --delete-branch` silently skips remote delete when local cleanup errors (worktree-in-use). Manual `git push origin --delete <b>` required afterward — caused 2 of the 42 stale-branch findings.
- The pt-BR `locales/pt-BR/package.json` is still flagged as `private: false` with `name: "ai-dev-toolkit"`. Standalone publish identity is undecided — dedicated decision needed before next pt-BR pass.

New follow-ups surfaced by PR #121 scoping:
- **`packages/setup/**` `cd ai-dev-toolkit-setup` snippets** (≈10 occurrences in `packages/setup/README*.md`, `OWNERSHIP.md`, `TOOLKIT_COMPARISON.md`, `docs/BRANCHES.md`). These were imported from the standalone setup repo and reference the old standalone-clone workflow. Need a dedicated setup-rewrite PR.
- **`locales/pt-BR/kit/**` skill content** with `ai-dev-toolkit/...` example paths (~6 occurrences). Belongs in a dedicated pt-BR pass.
- **`~/.config/ai-dev-toolkit/local`** config-dir paths (≈7 occurrences). Touching these risks breaking existing user installs — needs a deliberate config-dir migration plan, not a mechanical rename.

Anchored to existing prior art: `docs/roadmap.md`, `docs/specs/2026-04-22-toolkit-monorepo-rebrand`, `docs/specs/2026-04-23-dev-assets-import`, `docs/specs/2026-04-24-forge-space-knowledge-import`. Items below do not duplicate; they extend with concrete next PRs.

---

## Top 10 — best impact/effort right now

| # | Item | Bucket | Impact | Effort | Risk | Conf |
|---|------|--------|--------|--------|------|------|
| 1 | Bump `astro` ^5.18.1 → ^6.1.6 in `apps/web` to close GHSA-j687-52p2-xcff (XSS via `define:vars`) | G — Security | high | M | med | high |
| 2 | Internal-references PR: replace `ai-dev-toolkit` → `forgekit` across 205 files (post-rename task A6 from rebrand spec) | A — Now | high | M | low | high |
| 3 | Update `package.json` `repository.url` from `…/ai-dev-toolkit` → `…/forgekit` and any badge URLs in root README | A — Now | med | S | low | high |
| 4 | Promote `forge-space-knowledge-import` Phase A (docs-only) to a PR — 8 imports listed in `tasks.md` | A — Now | high | M | low | high |
| 5 | Open or prune 3 orphaned remote branches: `audit/external-repos-2026-04-22`, `feat/patterns-dair-ai-guide`, `feat/patterns-prompt-catalogs` (no PR, two diverged from local) | A — Now | med | S | med | high |
| 6 | Close out `dev-assets-import` task #2: remove hardcoded local `dev-assets` paths from repo scripts | B — Next | high | M | low | high |
| 7 | Bump `actions/setup-node@v4`, `pnpm/action-setup@v4`, `cloudflare/wrangler-action@v3` in `.github/workflows/deploy-web.yml` to Node 24-compatible versions before 2026-09-16 forced removal | H — Migration | med | S | low | high |
| 8 | Update `deploy-web.yml` comments + `CLOUDFLARE_ACCOUNT_ID` rationale to reference `forgekit` Worker, not `ai-dev-toolkit-library` | D — Tech debt | low | S | low | high |
| 9 | Remove stale `import-library` git remote pointing at `/tmp/forgekit-library.a4Hu4h/repo` (will fail on a fresh machine) | D — Tech debt | low | S | low | high |
| 10 | Archive stale handoff dir `~/.claude/handoffs/lucassantana-dev-ai-dev-toolkit/` (latest 9 d old, references already-merged `fix/nist-link`) | I — Docs | low | S | low | high |

---

## A. Now — this week, ≤1 PR each

### A1. Bump astro to ^6.1.6 in `apps/web` — quiets GHSA-j687-52p2-xcff (NOT exploitable here)
- **Why**: `pnpm audit` reports `astro@5.18.1` vulnerable to XSS via `define:vars`. Re-evaluation 2026-04-25:
  - **Not exploitable in this codebase**: zero `define:vars` usages in `apps/web/` (`grep -rn "define:vars" apps/web/` is empty).
  - **Output mode is `'static'`** (no `output:` set in `apps/web/astro.config.mjs`); the advisory explicitly requires SSR (`output: 'server'`).
  - Bumping is still worthwhile for audit hygiene, future-proofing, and Astro 6 perf/features — but is no longer "active security" urgency.
- **Evidence**: `pnpm audit --json`; `apps/web/astro.config.mjs` (no `output:`); GHSA-j687-52p2-xcff advisory text.
- **Next**: branch `chore(deps)/astro-6.1.6`. Read Astro 5→6 migration notes first (this is a major). Run `pnpm --filter @forge-kit/web up astro@^6.1.6`, build locally, deploy via existing CI. Verify the smoke check still hits 200 on the 3 canonical URLs.
- `[impact: med]` `[effort: M]` `[risk: med]` `[confidence: high]` _(was high impact; downgraded after exposure analysis)_

### A2. Internal-references rename: `ai-dev-toolkit` → `forgekit`
- **Why**: 205 files still reference the old name (counted via `grep -rln 'ai-dev-toolkit'`), including `package.json` `repository.url`, `locales/pt-BR/**` patterns, `docs/AI_ASSISTED_DEVELOPMENT_SUMMARY*.md`, `docs/for-teams.md`. Spec `2026-04-22-toolkit-monorepo-rebrand/tasks.md` already lists this as task §4 (unchecked).
- **Evidence**: file inventory above; `docs/specs/2026-04-22-toolkit-monorepo-rebrand/playbook-rename.md § 4`.
- **Next**: scripted `sed`-style rename PR with manual review of the locales (avoid breaking any URL that legitimately points at the old GitHub redirect).
- `[impact: high]` `[effort: M]` `[risk: low]` `[confidence: high]`

### A3. `package.json` repository URL + badges
- **Why**: `package.json:8` still says `https://github.com/LucasSantana-Dev/ai-dev-toolkit`. GitHub redirects work but tooling that hits the API directly (Renovate, badge services) breaks silently.
- **Evidence**: `cat package.json | head -10`.
- **Next**: 1-line edit; included in A2 if scope allows, else stand-alone.
- `[impact: med]` `[effort: S]` `[risk: low]` `[confidence: high]`

### A4. `forge-space-knowledge-import` Phase A — docs-only PR
- **Why**: Spec is already approved-shape (status: proposed → ready). 8 concrete imports listed; no schema or build changes; pure docs. Unblocks Phases B-E.
- **Evidence**: `docs/specs/2026-04-24-forge-space-knowledge-import/tasks.md` Phase A.
- **Blocker**: spec lists "User resolves the 4 Open Questions" as a pre-flight step. Verify those are answered before opening PR.
- **Next**: open PR `docs(architecture): import forge-space core + MCP architecture notes`.
- `[impact: high]` `[effort: M]` `[risk: low]` `[confidence: high]`

### A5. Triage 3 orphaned remote branches
- `audit/external-repos-2026-04-22` — local 2 commits AHEAD of remote (`e5f8e34` vs `8345b44`); content already merged via PR #94. Likely safe to delete remote ref.
- `feat/patterns-dair-ai-guide` — local at `5b62c48`, remote at `19d0b6d` — content shipped via PR #95. Likely safe to delete remote ref.
- `feat/patterns-prompt-catalogs` — in sync at `b77fa8d`; content shipped via PR #93. Safe to delete.
- **Next**: confirm each is fully merged, then `git push origin --delete` for each.
- **Evidence**: `gh pr list` empty; recent commit log shows `(#93)`, `(#94)`, `(#95)` already on `main`.
- `[impact: med]` `[effort: S]` `[risk: med]` `[confidence: high]`

---

## B. Next — next 2 weeks

### B1. `dev-assets-import` — remove hardcoded local paths
- **Why**: Spec task #2. Setup scripts currently depend on a private machine-specific backup repo. Blocks anyone else running setup from a clean clone.
- **Evidence**: `docs/specs/2026-04-23-dev-assets-import/spec.md`. PR #97 already removed one path; the spec lists more.
- **Next**: audit `packages/setup/**` and any `scripts/` referencing absolute `/Volumes/External HD/...` or `dev-assets` paths.
- `[impact: high]` `[effort: M]` `[risk: low]` `[confidence: high]`

### B2. `forge-space-knowledge-import` Phase B — catalog server entries
- **Why**: Adds `branding-mcp.yaml` and `ui-mcp.yaml` server entries. Optional `usage` block depends on PR #102 (already merged), so unblocked.
- **Evidence**: `docs/specs/2026-04-24-forge-space-knowledge-import/tasks.md` Phase B.
- **Blocker**: Phase A must merge first.
- `[impact: med]` `[effort: M]` `[risk: low]` `[confidence: high]`

### B3. Archive 3 source repos (rebrand spec final step)
- **Why**: Spec task §5: `gh repo archive` `ai-dev-toolkit-setup`, `-pt-br`, `-library` after one green monorepo release. v0.17.0 shipped, all CI green on HEAD — unblocked.
- **Evidence**: `docs/specs/2026-04-22-toolkit-monorepo-rebrand/playbook-archive-source-repos.md`.
- **Risk**: medium — irreversible-ish (un-archive is possible but disruptive). Confirm no external consumers still pinning to those repos first.
- `[impact: med]` `[effort: M]` `[risk: med]` `[confidence: high]`

---

## C. Bug fixes / incidents

### C1. None active
- All `Validate` failures in last 14 d (run IDs `24689015061`, `24674963419`, `24542069594` and 9 others on 2026-04-13) — already remediated; HEAD is green.
- `Deploy webapp` failures (run IDs `24916551267`, `24916518386`, `24916502214`) all from 2026-04-24 — already fixed by PRs #114, #115. Latest deploy run `24920440883` succeeded.
- `[confidence: high]` that no live bugs exist.

---

## D. Tech debt

### D1. `import-library` stray git remote
- **Why**: `git remote -v` shows `import-library → /tmp/forgekit-library.a4Hu4h/repo` (fetch + push). Temp dir likely deleted; remote will fail on first `git fetch import-library`. Cosmetic on this machine, broken on any other clone.
- **Evidence**: `git remote -v` output.
- **Next**: `git remote remove import-library`.
- **What breaks if not fixed**: confusing error if anyone runs `git fetch --all`.
- `[impact: low]` `[effort: S]` `[risk: low]` `[confidence: high]`

### D2. `deploy-web.yml` outdated comments
- **Why**: Header comment says "the account that owns `ai-dev-toolkit-library`" — that repo is being archived (B3). Confusing for future maintainers.
- **Evidence**: `.github/workflows/deploy-web.yml:7-12`.
- **Next**: 5-line comment edit. Bundle with A2 rename PR.
- `[impact: low]` `[effort: S]` `[risk: low]` `[confidence: high]`

### D3. Dual locale tree maintenance burden
- **Why**: `locales/pt-BR/` holds 457 files, fully duplicated tree (patterns, kit, scripts, tests). PR #110 backfilled translations for 126 skills/hooks; PR #111 added a parity validator. Cost: every English change risks parity drift.
- **Evidence**: `find locales/pt-BR -type f | wc -l`, recent commits #110 and #111.
- **Next**: not a fix today — file as a debt-monitoring item. Consider a single-source-of-truth approach (i18n strings in JSON + generator) if drift becomes painful.
- **What breaks if not addressed**: validator catches drift but does not auto-fix; reviewer time grows linearly with content.
- `[impact: med]` `[effort: L]` `[risk: med]` `[confidence: med]`

### D5. 40 stale remote branches
- **Why**: `git ls-remote --heads origin` shows 40 non-`main`, non-`release/*`, non-`personal` branches. All look squash-merged but were never deleted because the `gh pr merge --delete-branch` flow silently skips the remote delete when local cleanup errors (most often when the branch is checked out in a worktree).
- **Evidence**: live `git ls-remote --heads origin` output 2026-04-25.
- **Next**: cron-style cleanup — for each branch, verify `git log origin/main..origin/<branch>` is empty OR the squash-equivalent commit landed (`gh pr list --state merged --search 'head:<branch>'`), then `git push origin --delete <branch>`. Could be a 30-line shell script or a one-time skill.
- **What breaks if not addressed**: `git ls-remote` and `gh` listings get noisy; new contributors see 40+ apparent in-flight branches; some IDE branch pickers truncate at N entries.
- `[impact: low]` `[effort: M]` `[risk: low]` `[confidence: high]`

### D4. Apparent TODO/FIXME hotspots are false positives
- **Why**: Top hotspot files (`packages/catalog/catalog/hooks/pre-commit-validation/script.sh`, `packages/core/kit/hooks/evaluate-response.sh`, `docs/guides/hooks.md`) match `TODO|FIXME` only because they grep for those strings as part of their feature. No actionable debt here.
- **Evidence**: hand-inspected `git grep -nE "TODO|FIXME|HACK"` output for each top hotspot.
- **Next**: nothing — documented so the next audit doesn't re-flag.
- `[impact: low]` `[effort: S]` `[risk: low]` `[confidence: high]`

---

## E. Refactor opportunities

### E1. `.github/workflows/deploy-web.yml` churn signal
- **Why**: 4 changes in last 90 d (`#112`, `#113`, `#114`, `#115`) — all CI fix-ups for Cloudflare/wrangler. Indicates fragile setup. After A1 (astro bump) settles, audit whether the workflow can be flattened (one source of `wrangler` version, one source of node setup).
- **Evidence**: `git log --since=90.days deploy-web.yml`.
- **Next**: only if it churns again — bias toward leaving alone now that latest run is green.
- `[impact: low]` `[effort: M]` `[risk: med]` `[confidence: med]`

---

## F. Performance / optimization

- **None with measurable wins**. Repo is content/tooling, not a runtime hot path. CI run times on `Deploy webapp` are 39-43 s (acceptable). `Validate` and `Semgrep` on HEAD complete in seconds. Flag "needs baseline" only if web-app TTFB or build time becomes a concern.

---

## G. Security

### G1. Astro XSS — see A1
- Originally surfaced as a security item. Re-evaluation 2026-04-25 confirmed the codebase is **not exploitable** through this vector (no `define:vars`, static output). Reclassified to hygiene; tracked in A1.

### G2. Dependabot + secret scanning are disabled at the repo level
- **Why**: `gh api /repos/.../dependabot/alerts` returns 403 ("disabled"). Same for secret-scanning. The repo runs Semgrep + a `secret-scan` GitHub Action workflow + the new gitleaks pre-commit (PR #116), so coverage exists — but enabling Dependabot alerts gives a free second signal.
- **Evidence**: API responses above; `.github/workflows/secret-scan.yml`, `.github/workflows/semgrep.yml` exist.
- **Next**: open repo Settings → Code security → enable Dependabot alerts + Dependabot security updates. Optional: enable secret scanning push protection (private repo cost may apply).
- `[impact: med]` `[effort: S]` `[risk: low]` `[confidence: high]`

---

## H. Major-version migrations (deferred)

### H1. Node 20 actions deprecation
- **Why**: GitHub Actions warning on every `Deploy webapp` run: "Node.js 20 actions are deprecated. The following actions are running on Node.js 20 and may not work as expected: actions/setup-node@v4, cloudflare/wrangler-action@v3, pnpm/action-setup@v4." Forced to Node 24 by 2026-06-02; Node 20 removed 2026-09-16.
- **Evidence**: GH Actions warning text in failed run logs (`gh run view 24916551267 --log-failed`); `.github/workflows/deploy-web.yml` confirmed as the only affected workflow.
- **Migration cost estimate**: low — bump `actions/setup-node@v5`, check `cloudflare/wrangler-action` for Node 24 release, `pnpm/action-setup@v5` if available. Smoke-test on a branch.
- **CVE risk of staying put**: rises sharply after 2026-09-16 when runner removes Node 20.
- `[impact: med]` `[effort: S]` `[risk: low]` `[confidence: high]`

### H2. Astro 5 → 6
- Covered in A1.

---

## I. Documentation

### I1. `docs/roadmap.md` is stale relative to spec set
- **Why**: Roadmap auto-generated 2026-04-22 lists only the rebrand and dev-assets specs. Missing the `2026-04-24-forge-space-knowledge-import` spec, which exists on disk.
- **Evidence**: `docs/roadmap.md:5-11` vs `ls docs/specs/`.
- **Next**: re-run the auto-generator (whatever produced the file) or hand-update.
- `[impact: low]` `[effort: S]` `[risk: low]` `[confidence: high]`

### I2. `CONTRIBUTING.md` last touched 6 weeks ago
- **Why**: Pre-monorepo. Likely still references old layout, single-package commands.
- **Evidence**: `git log -1 CONTRIBUTING.md`.
- **Next**: 30-min pass to align with monorepo `pnpm --filter` patterns and the new `forgekit` name.
- `[impact: med]` `[effort: S]` `[risk: low]` `[confidence: med]`

### I3. Stale handoff
- See **Top 10 #10**.

---

## J. Out of scope / won't fix

- **`dispatch_failures` workflow `.github/workflows/stale.yml`** — one failure 14 d ago (`24662320304`); appears to be a one-off GitHub Actions hiccup on the auto-stale job, not a code issue. Skip unless it recurs.
- **`github_actions in /. - Update #1332308695` failure** (`24751115461`) — Renovate/Dependabot meta-job; skip, not actionable here.
- **D3 i18n single-source rewrite** — flagged in D but parked: high effort, no acute pain yet, validator (#111) catches drift.

---

## Cross-cutting / unblocks

- **A2 (rename refs)** unblocks **D2 (deploy-web.yml comments)** and removes friction for **B3 (archive source repos)** since redirect README in those repos can point at `forgekit` cleanly.
- **A4 (forge-space Phase A)** unblocks **B2 (Phase B) → C/D/E** cascade in that spec.
- **G2 (enable Dependabot)** would have surfaced A1 days earlier — meta-improvement.

---

## Data sources / unknowns

- Counted: `git log`, `git grep`, `gh pr list`, `gh issue list`, `gh run list`, `pnpm audit --json`, `pnpm outdated -r`, `find` on locales tree.
- **Could not pull**: GitHub Dependabot alerts (`403 disabled`) — re-check once G2 lands.
- **Skipped**: full `npm outdated` per-workspace deep dive — `pnpm outdated -r` returned only 1 outdated indicator (likely Astro). Confirm on a follow-up audit.
- **No coverage report exists** in repo — test count = 4 test files in root + locales mirror. Skipped trend analysis.
