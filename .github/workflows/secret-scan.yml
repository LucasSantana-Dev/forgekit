name: Secret Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  secret-scan:
    uses: Forge-Space/github-org-workflows/.github/workflows/reusable-secret-scan.yml@main
